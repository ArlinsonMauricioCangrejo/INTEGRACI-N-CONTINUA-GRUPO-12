# Variables de entorno del Sistema de Gestión de Tickets
# Copiá este archivo a .env (que está en .gitignore) y ajustá los valores.

POSTGRES_USER=ticket_user
POSTGRES_PASSWORD=ticket_pass
POSTGRES_DB=tickets_db
